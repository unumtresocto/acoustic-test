export function getHeading(article) {
  return article.elements?.heading;
}

export function getAuthor(article) {
  return article.elements?.author;
}

export function getBody(article) {
  return article.elements?.body;
}

export function getDate(article) {
  return article.elements?.date;
}

export function getMainImage(article) {
  return article.elements?.mainImage;
}

export function getId(article) {
  return article.elements?.id;
}
